import { objectFlatten } from "../src/misc";
describe('objectFlatten tests', function () {
    test('objectFlatten level 1', function () {
        var obj = {
            a: 9
        };
        var res = objectFlatten(obj);
        var expected = [{
                searchField: 'a',
                searchValue: 9
            }];
        expect(res).toEqual(expected);
    });
    test('objectFlatten level 2', function () {
        var obj = {
            a: 9,
            apple: {
                value: 1
            }
        };
        var res = objectFlatten(obj);
        var expected = [{
                searchField: 'a',
                searchValue: 9
            }, {
                searchField: 'apple.value',
                searchValue: 1
            }];
        expect(res).toEqual(expected);
    });
});
//# sourceMappingURL=objectFlatten.spec.js.map