import path from "path-browserify";
export function getAbsolutePath(rootRef, relativePath) {
    if (!rootRef) {
        return relativePath + '';
    }
    if (!relativePath) {
        throw new Error("Resource name must be a string of length greater than 0 characters");
    }
    var rootRefValue = typeof rootRef === "string" ? rootRef : rootRef();
    var withSlashes = path.join("/", rootRefValue, "/", relativePath, "/");
    var slashCount = withSlashes.split("/").length - 1;
    if (slashCount % 2) {
        throw new Error("The rootRef path must point to a \"document\"\n    not a \"collection\"e.g. /collection/document/ or\n    /collection/document/collection/document/");
    }
    return withSlashes.slice(1, -1);
}
export function joinPaths() {
    var args = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        args[_i] = arguments[_i];
    }
    return path.join.apply(path, args);
}
//# sourceMappingURL=pathHelper.js.map