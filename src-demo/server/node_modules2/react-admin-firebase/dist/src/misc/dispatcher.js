import { log } from './logger';
export function dispatch(eventName, fileName, data) {
    var eventMonitor = document.getElementById('eventMonitor');
    if (!eventMonitor) {
        log("eventMonitor not found to dispatch event ".concat(eventName, " for ").concat(fileName));
        return;
    }
    var eventData = { fileName: fileName, data: data };
    var event = new CustomEvent(eventName, { detail: eventData });
    eventMonitor.dispatchEvent(event);
}
;
//# sourceMappingURL=dispatcher.js.map