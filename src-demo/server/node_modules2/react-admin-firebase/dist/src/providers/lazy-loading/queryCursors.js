var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
export function setQueryCursor(doc, params, resourceName) {
    var key = btoa(JSON.stringify(__assign(__assign({}, params), { resourceName: resourceName })));
    localStorage.setItem(key, doc.id);
    var allCursorsKey = "ra-firebase-cursor-keys_".concat(resourceName);
    var localCursorKeys = localStorage.getItem(allCursorsKey);
    if (!localCursorKeys) {
        localStorage.setItem(allCursorsKey, JSON.stringify([key]));
    }
    else {
        var cursors = JSON.parse(localCursorKeys);
        var newCursors = cursors.concat(key);
        localStorage.setItem(allCursorsKey, JSON.stringify(newCursors));
    }
}
export function getQueryCursor(collection, params, resourceName, flogger) {
    return __awaiter(this, void 0, void 0, function () {
        var key, docId, doc;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    key = btoa(JSON.stringify(__assign(__assign({}, params), { resourceName: resourceName })));
                    docId = localStorage.getItem(key);
                    if (!docId) {
                        return [2 /*return*/, false];
                    }
                    return [4 /*yield*/, collection.doc(docId).get()];
                case 1:
                    doc = _a.sent();
                    flogger.logDocument(1)();
                    if (doc.exists) {
                        // incrementFirebaseReadsCounter(1);
                        return [2 /*return*/, doc];
                    }
                    return [2 /*return*/, false];
            }
        });
    });
}
export function clearQueryCursors(resourceName) {
    var allCursorsKey = "ra-firebase-cursor-keys_".concat(resourceName);
    var localCursorKeys = localStorage.getItem(allCursorsKey);
    if (localCursorKeys) {
        var cursors = JSON.parse(localCursorKeys);
        cursors.forEach(function (cursor) { return localStorage.removeItem(cursor); });
        localStorage.removeItem(allCursorsKey);
    }
}
export function findLastQueryCursor(collection, queryBase, params, resourceName, flogger) {
    return __awaiter(this, void 0, void 0, function () {
        function getQuery() {
            if (isFirst) {
                return queryBase.limit(limit);
            }
            else {
                return queryBase.startAt(lastQueryCursor).limit(limit);
            }
        }
        var _a, page, perPage, lastQueryCursor, currentPage, currentPageParams, limit, isFirst, newQuery, snapshots, docsLength, lastDocIndex, lastDocRef;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    _a = params.pagination, page = _a.page, perPage = _a.perPage;
                    lastQueryCursor = false;
                    currentPage = page - 1;
                    currentPageParams = __assign(__assign({}, params), { pagination: __assign({}, params.pagination) });
                    _b.label = 1;
                case 1:
                    if (!(!lastQueryCursor && currentPage > 1)) return [3 /*break*/, 3];
                    currentPage--;
                    currentPageParams.pagination.page = currentPage;
                    console.log('getting query cursor currentPage=', currentPage);
                    return [4 /*yield*/, getQueryCursor(collection, currentPageParams, resourceName, flogger)];
                case 2:
                    lastQueryCursor = _b.sent();
                    return [3 /*break*/, 1];
                case 3:
                    limit = (page - currentPage) * perPage;
                    isFirst = currentPage === 1;
                    newQuery = getQuery();
                    return [4 /*yield*/, newQuery.get()];
                case 4:
                    snapshots = _b.sent();
                    docsLength = snapshots.docs.length;
                    flogger.logDocument(docsLength)();
                    lastDocIndex = docsLength - 1;
                    lastDocRef = snapshots.docs[lastDocIndex];
                    return [2 /*return*/, lastDocRef];
            }
        });
    });
}
//# sourceMappingURL=queryCursors.js.map